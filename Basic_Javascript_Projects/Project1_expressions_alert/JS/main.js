// Declare and initialize the Sent1 variable with a string value
var sent1 = "This is the beginning of the string";

// Declare and initialize the Sent2 variable with a string value
var sent2 = " and this is the end of the string";

// Concatenate Sent1 and Sent2 variables together
var result = sent1 + sent2;

// Display the concatenated string using the document.write() method
document.write("The concatenated string is: " + result);

// Display an alert with the concatenated string using the window.alert() method
window.alert("The concatenated string is: " + result);


